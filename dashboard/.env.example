# The dashboard container is a gateway: it serves the SvelteKit UI and
# reverse-proxies /api (HTTP) and the WS upgrade to the bot container.
# Configure the bot's internal address here (defaults shown).
BOT_HOST=paxfax
BOT_HTTP_PORT=7781
BOT_WS_PORT=7780
