# Browser-reachable URLs of the bot's in-process dashboard servers.
# In dev: localhost. In deploy: the reverse-proxy HTTPS URLs.
PUBLIC_DASHBOARD_WS_URL=ws://localhost:7780
PUBLIC_DASHBOARD_HTTP_URL=http://localhost:7781
